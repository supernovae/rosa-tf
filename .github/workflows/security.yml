# Security and Quality Checks
# Runs on all PRs and pushes to main

name: Security Checks

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  security-events: write

jobs:
  netapp-tests:
    name: NetApp storage security and release contracts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.16.1"
          terraform_wrapper: false
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: terraform -chdir=tests/netapp test
      - run: python -m pip install PyYAML==6.0.2 python-hcl2==8.1.4
      - run: python scripts/check-netapp-contracts.py
      - run: terraform -chdir=modules/gitops-layers/netapp-storage init -backend=false -lockfile=readonly
      - run: terraform -chdir=modules/gitops-layers/netapp-storage test

  gitops-tests:
    name: GitOps security contracts and release schemas
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.16.1"
          terraform_wrapper: false
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: terraform -chdir=tests/gitops test
      - run: python -m unittest discover -s tests/gitops -p 'test_*.py'
      - run: python -m pip install PyYAML==6.0.2 jsonschema==4.23.0 python-hcl2==8.1.4
      - run: python scripts/check-gitops-schemas.py

  monitoring-tests:
    name: Monitoring templates and release schemas
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.16.1"
          terraform_wrapper: false
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: terraform -chdir=tests/monitoring test
      - run: python -m pip install PyYAML==6.0.2 jsonschema==4.23.0
      - run: python scripts/check-monitoring-schemas.py
      - name: Initialize mocked monitoring storage tests
        run: terraform -chdir=modules/gitops-layers/monitoring init -backend=false -lockfile=readonly
      - name: Verify retention, encryption and partition boundaries
        run: terraform -chdir=modules/gitops-layers/monitoring test

  certmanager-tests:
    name: cert-manager templates and release schemas
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.16.1"
          terraform_wrapper: false
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Test template behavior
        run: terraform -chdir=tests/certmanager test
      - name: Install schema checker dependencies
        run: python -m pip install PyYAML==6.0.2 jsonschema==4.23.0
      - name: Check pinned release schemas
        run: python scripts/check-certmanager-schemas.py

  network-tests:
    name: Zero-egress network regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.16.1"
          terraform_wrapper: false
      - name: Initialize network tests
        working-directory: modules/networking/vpc
        run: terraform init -backend=false -lockfile=readonly
      - name: Verify isolated and NAT network plans
        working-directory: modules/networking/vpc
        run: terraform test

  example-contracts:
    name: Example input contracts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install pinned HCL parser
        run: python -m pip install python-hcl2==8.1.4
      - name: Check sample inputs and deployment invariants
        run: python scripts/check-examples.py

  #----------------------------------------------------------------------------
  # Shell Script Security
  #----------------------------------------------------------------------------
  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: "."
          severity: warning
          format: gcc
        env:
          SHELLCHECK_OPTS: -x -e SC1091

  #----------------------------------------------------------------------------
  # Terraform Security Scanning
  # tfsec + Checkov provide comprehensive IaC coverage.
  #----------------------------------------------------------------------------
  tfsec:
    name: tfsec Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install tfsec
        run: |
          curl -sLo tfsec https://github.com/aquasecurity/tfsec/releases/latest/download/tfsec-linux-amd64
          chmod +x tfsec
          sudo mv tfsec /usr/local/bin/

      - name: Run tfsec
        run: |
          tfsec . --format sarif --out tfsec-results.sarif --soft-fail || true
          if [ ! -s tfsec-results.sarif ]; then
            echo '{"version":"2.1.0","$schema":"https://raw.githubusercontent.com/oasis-tcs/sarif-spec/main/Schemata/sarif-schema-2.1.0.json","runs":[{"tool":{"driver":{"name":"tfsec","rules":[]}},"results":[]}]}' > tfsec-results.sarif
          fi

      - name: Upload tfsec results
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: tfsec-results.sarif

  #----------------------------------------------------------------------------
  # Vulnerability Scanning
  #----------------------------------------------------------------------------
  grype:
    name: Grype Vulnerability Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run Grype filesystem scan
        uses: anchore/scan-action@v7
        id: grype
        with:
          path: "."
          fail-build: false
          severity-cutoff: high
          output-format: sarif
          output-file: grype-results.sarif

      - name: Upload Grype results
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: grype-results.sarif

  checkov:
    name: Checkov
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run Checkov
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: .
          framework: terraform
          soft_fail: true
          output_format: sarif
          output_file_path: checkov-results.sarif
          config_file: .checkov.yml

      - name: Upload Checkov results
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: checkov-results.sarif

  #----------------------------------------------------------------------------
  # Secrets Detection
  #----------------------------------------------------------------------------
  gitleaks:
    name: Gitleaks Secret Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2.3.9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  trufflehog:
    name: TruffleHog Secret Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Run TruffleHog
        uses: trufflesecurity/trufflehog@main
        with:
          extra_args: --only-verified

  #----------------------------------------------------------------------------
  # Terraform Validation
  #----------------------------------------------------------------------------
  terraform-validate:
    name: Terraform Validate
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment:
          - commercial-classic
          - commercial-hcp
          - govcloud-classic
          - govcloud-hcp
    steps:
      - uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.16.1"
          terraform_wrapper: false

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        working-directory: environments/${{ matrix.environment }}
        run: terraform init -backend=false -lockfile=readonly

      - name: Terraform Validate
        working-directory: environments/${{ matrix.environment }}
        run: terraform validate -json | jq -e 'if .valid and .warning_count == 0 then . else error(tojson) end'

  #----------------------------------------------------------------------------
  # YAML Linting
  #----------------------------------------------------------------------------
  bootstrap-tests:
    name: Bootstrap migration (${{ matrix.cluster_type }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cluster_type: [classic, hcp]
    steps:
      - uses: actions/checkout@v6
      - uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.16.1"
          terraform_wrapper: false
      - name: Initialize test module
        working-directory: modules/cluster/rosa-${{ matrix.cluster_type }}
        run: terraform init -backend=false -lockfile=readonly
      - name: Test native bootstrap and legacy state migration
        working-directory: modules/cluster/rosa-${{ matrix.cluster_type }}
        run: bash ../../../scripts/test-bootstrap.sh

  yaml-lint:
    name: YAML Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run yamllint
        uses: ibiqlik/action-yamllint@v3
        with:
          file_or_dir: "."
          config_file: .yamllint.yml
        continue-on-error: true

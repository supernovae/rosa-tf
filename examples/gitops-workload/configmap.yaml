# Namespace intentionally omitted: the Application supplies its delegated destination.
apiVersion: v1
kind: ConfigMap
metadata:
  name: gitops-acceptance
data:
  message: "Reviewed workload sync succeeded"

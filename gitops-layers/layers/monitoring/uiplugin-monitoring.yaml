# Requires COO >=1.5 for GA Perses. See docs/OBSERVABILITY.md catalog preflight.
apiVersion: observability.openshift.io/v1alpha1
kind: UIPlugin
metadata:
  name: monitoring
spec:
  type: Monitoring
  monitoring:
    perses:
      enabled: true

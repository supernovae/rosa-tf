${yamlencode({
  apiVersion = "operators.coreos.com/v1alpha1"
  kind = "Subscription"
  metadata = { name = "redhat-oadp-operator", namespace = "openshift-adp" }
  spec = {
    channel = "stable"
    name = "redhat-oadp-operator"
    source = config.catalog_source
    sourceNamespace = config.catalog_namespace
    installPlanApproval = config.install_plan_approval
    config = { env = [{ name = "ROLEARN", value = role_arn }] }
  }
})}

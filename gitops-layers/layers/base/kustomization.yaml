apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: layer-base
  annotations:
    rosa-gitops-layer: base
    rosa-gitops-description: "Base cluster configurations applied to all ROSA clusters"

# Base layer is intentionally minimal
# Add common configurations here that should apply to all clusters
# Examples: NetworkPolicies, RBAC, resource quotas, etc.

resources: []
# Uncomment and add resources as needed:
# - cluster-config/
# - rbac/
# - network-policies/

commonLabels:
  app.kubernetes.io/part-of: rosa-gitops-layers
  app.kubernetes.io/component: base
  rosa-gitops-layer: base
